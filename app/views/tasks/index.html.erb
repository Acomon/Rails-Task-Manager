<h1>Tasks-index</h1>

<%= link_to 'New task', new_task_path %>


<ol>
	<% @tasks.each do |task| %>
	<li><%= "name: " %> <%=link_to task.name, task_path(task) %> <br>
		<%= link_to 'Edit', edit_task_path(task) %>
		<%= link_to 'Delete', task_path(task), method: :delete %>
	</li>
	<br>
	<%end%>

</ol>
